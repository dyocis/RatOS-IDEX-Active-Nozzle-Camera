name: Validate

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate shell scripts
        run: |
          bash -n install.sh
          bash -n uninstall.sh
          bash -n verify.sh
          bash -n tests/test_upgrade.sh
          bash -n tests/test_install_uninstall.sh
      - name: Validate Python
        run: python3 -m py_compile src/active_nozzle_camera.py
      - name: Run functional tests
        run: python3 -m unittest discover -s tests -p 'test_*.py' -v
      - name: Run install/uninstall test
        run: bash tests/test_install_uninstall.sh
      - name: Run upgrade test
        run: bash tests/test_upgrade.sh
